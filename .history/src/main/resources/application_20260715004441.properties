spring.application.name=FoodBridge

# Database Configuration
spring.datasource.url=${jdbc:mysql://hayabusa.proxy.rlwy.net:48318/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${root}
spring.datasource.password=${RbzpmHISdSFhGZeczvSupiaDdllXsTOZ}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=${PORT:8080}